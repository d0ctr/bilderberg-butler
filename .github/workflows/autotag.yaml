name: 'Autotag'

on: 
  push:
    branches:
      - main

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create Tag
        id: create_tag
        uses: jaywcjlove/create-tag-action@main
